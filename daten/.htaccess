# Dieser Ordner enthält die verschlüsselte Datenbank und den Schlüssel.
# Nichts davon darf je über das Web abrufbar sein.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>

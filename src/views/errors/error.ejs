<%- include('../partials/head', { bodyClass: 'gate-page' }) %>

<main class="gate-wrap">
  <div class="gate-card center">
    <div class="error-status"><%= typeof status !== 'undefined' ? status : 500 %></div>
    <h1><%= title %></h1>
    <p class="muted"><%= message %></p>
    <div class="hero-cta">
      <a class="btn btn-primary" href="<%- basePath %>">Zur Startseite</a>
      <a class="btn btn-ghost" href="<%- basePath %>/shop">Zum Shop</a>
    </div>
  </div>
</main>

</body>
</html>
